diff --git OLD/rpm.spec NEW/rpm.spec
index 9c708b1..e3a5390 100644
--- OLD/rpm.spec
+++ NEW/rpm.spec
@@ -430,7 +430,7 @@ fi
 
 %files -f _build/rpm.lang
 %license COPYING
-%doc CREDITS docs/manual/[a-z]*
+%doc CREDITS
 %doc %{_defaultdocdir}/rpm/CONTRIBUTING.md
 %doc %{_defaultdocdir}/rpm/COPYING
 %doc %{_defaultdocdir}/rpm/INSTALL
@@ -473,22 +473,13 @@ fi
 
 %attr(0755, root, root) %dir %{rpmhome}
 %{rpmhome}/macros
-%exclude %{rpmhome}/macros.d/macros.transaction*
-%{rpmhome}/macros.d
-%{rpmhome}/lua
 %{rpmhome}/rpmpopt*
 %{rpmhome}/rpmrc
 
-%{rpmhome}/rpmdb_*
-%{rpmhome}/rpm.daily
-%{rpmhome}/rpm.log
-%{rpmhome}/rpm.supp
-%{rpmhome}/rpm2cpio.sh
-%{rpmhome}/tgpg
-
-%{rpmhome}/platform
 %{rpmhome}/sysusers.sh
 
+%dir %{rpmhome}/macros.d
+%dir %{rpmhome}/lua
 %dir %{rpmhome}/fileattrs
 
 %files libs
@@ -564,6 +555,7 @@ fi
 %{_libdir}/librpmsign.so.%{sover}.*
 
 %files build
+%doc %{_defaultdocdir}/rpm/[a-z]*
 %{_bindir}/rpmbuild
 %{_bindir}/gendiff
 %{_bindir}/rpmspec
@@ -591,6 +583,12 @@ fi
 %{rpmhome}/rpmuncompress
 %{rpmhome}/rpmdump
 %{rpmhome}/rpm-setup-autosign
+%{rpmhome}/rpm.supp
+%{rpmhome}/rpmdb_*
+%{rpmhome}/rpm2cpio.sh
+%{rpmhome}/tgpg
+
+%{rpmhome}/platform
 
 %files sign
 %{_bindir}/rpmsign
@@ -616,6 +614,9 @@ fi
 %{_includedir}/rpm/
 
 %files cron
+%{rpmhome}/rpm.daily
+%{rpmhome}/rpm.log
+
 %{_sysconfdir}/cron.daily/rpm
 %config(noreplace) %{_sysconfdir}/logrotate.d/rpm
 
