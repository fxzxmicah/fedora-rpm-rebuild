diff --git OLD/wireplumber.spec NEW/wireplumber.spec
index 6cbc47e..0ba9d0e 100644
--- OLD/wireplumber.spec
+++ NEW/wireplumber.spec
@@ -32,9 +32,27 @@ BuildRequires:  systemd-rpm-macros
 # Make sure that we have -libs package in the same version
 Requires:       %{name}-libs%{?_isa} = %{version}-%{release}
 
+Requires:       (%{name}-zsh-completion = %{version}-%{release} if zsh)
+
 Provides:       pipewire-session-manager
 Conflicts:      pipewire-session-manager
 
+%description
+WirePlumber is a modular session/policy manager for PipeWire and a
+GObject-based high-level library that wraps PipeWire's API, providing
+convenience for writing the daemon's modules as well as external tools for
+managing PipeWire.
+
+%package zsh-completion
+Summary:        Zsh completion for %{name}
+Requires:       %{name} = %{version}-%{release}
+Requires:       zsh
+
+BuildArch:      noarch
+
+%description zsh-completion
+This package contains zsh completion for %{name}.
+
 %package        libs
 Summary:        Libraries for WirePlumber clients
 Recommends:     %{name}%{?_isa} = %{version}-%{release}
@@ -59,12 +77,6 @@ Recommends:     %{name}%{?_isa} = %{version}-%{release}
 %description doc
 This package contains the documentation for WirePlumber.
 
-%description
-WirePlumber is a modular session/policy manager for PipeWire and a
-GObject-based high-level library that wraps PipeWire's API, providing
-convenience for writing the daemon's modules as well as external tools for
-managing PipeWire.
-
 %prep
 %autosetup -p1
 
@@ -111,11 +123,13 @@ fi
 %dir %{_sysconfdir}/wireplumber/main.lua.d
 %dir %{_sysconfdir}/wireplumber/policy.lua.d
 %{_datadir}/wireplumber/
-%{_datadir}/zsh/site-functions/_wpctl
 %{_datadir}/bash-completion/completions/wpctl
 %{_userunitdir}/wireplumber.service
 %{_userunitdir}/wireplumber@.service
 
+%files zsh-completion
+%{_datadir}/zsh/site-functions/*
+
 %files libs -f %{name}.lang
 %license LICENSE
 %dir %{_libdir}/wireplumber-0.5/
