diff --git OLD/gnome-control-center.spec NEW/gnome-control-center.spec
index dfebc4a..1d6e7c6 100644
--- OLD/gnome-control-center.spec
+++ NEW/gnome-control-center.spec
@@ -13,8 +13,10 @@
 
 %global tarball_version %%(echo %{version} | tr '~' '.')
 
-# Disable parental control for RHEL builds
-%bcond malcontent %[!0%{?rhel}]
+# Disable malcontent for all builds
+%bcond malcontent 0
+# Disable samba for all builds
+%bcond samba 0
 
 Name:           gnome-control-center
 Version:        50.1
@@ -25,6 +27,10 @@ License:        GPL-2.0-or-later AND CC0-1.0
 URL:            https://gitlab.gnome.org/GNOME/gnome-control-center/
 Source0:        https://download.gnome.org/sources/%{name}/50/%{name}-%{tarball_version}.tar.xz
 
+# Samba dep patches
+Patch0:         https://gitlab.gnome.org/GNOME/gnome-control-center/-/merge_requests/3118.patch
+Patch1:         https://gitlab.gnome.org/fxzxmicah/gnome-control-center/-/commit/bf023127ca8e3ede0723395818b3baf4d03765e5.patch
+
 BuildRequires:  blueprint-compiler >= %{blueprint_compiler_version}
 BuildRequires:  desktop-file-utils
 BuildRequires:  docbook-style-xsl libxslt
@@ -63,7 +69,9 @@ BuildRequires:  pkgconfig(malcontent-0)
 BuildRequires:  pkgconfig(mm-glib)
 BuildRequires:  pkgconfig(polkit-gobject-1)
 BuildRequires:  pkgconfig(pwquality)
+%if %{with samba}
 BuildRequires:  pkgconfig(smbclient)
+%endif
 BuildRequires:  pkgconfig(tecla)
 BuildRequires:  pkgconfig(udisks2)
 BuildRequires:  pkgconfig(upower-glib) >= %{upower_version}
@@ -122,10 +130,8 @@ Requires: /usr/bin/tecla
 %if 0%{?fedora} >= 35 || 0%{?rhel} >= 9
 # For the power panel
 Recommends: ppd-service
-%if 0%{?fedora} && 0%{?fedora} < 41
+%if 0%{?fedora}
 Suggests: power-profiles-daemon
-%else
-Suggests: tuned-ppd
 %endif
 %endif
 
@@ -155,6 +161,13 @@ for applications. This package contains directories where applications
 can install configuration files that are picked up by the control-center
 utilities.
 
+%package devel
+Summary: Development files for GNOME Control Center
+Requires: %{name}-filesystem = %{version}-%{release}
+
+%description devel
+This package contains development files for the GNOME Control Center.
+
 %prep
 %autosetup -p1 -n %{name}-%{tarball_version}
 
@@ -174,6 +187,11 @@ utilities.
   -Dmalcontent=true \
 %else
   -Dmalcontent=false \
+%endif
+%if %{with samba}
+  -Dsamba=enabled \
+%else
+  -Dsamba=disabled \
 %endif
   %{nil}
 %meson_build
@@ -210,7 +228,6 @@ rm -rf $RPM_BUILD_ROOT%{_datadir}/gnome/cursor-fonts
 %{_mandir}/man1/gnome-control-center.1*
 %{_metainfodir}/org.gnome.Settings.metainfo.xml
 %{_datadir}/pixmaps/faces
-%{_datadir}/pkgconfig/gnome-keybindings.pc
 %{_datadir}/polkit-1/actions/org.gnome.controlcenter.*.policy
 %{_datadir}/polkit-1/rules.d/gnome-control-center.rules
 %{_datadir}/sounds/gnome/default/*/*.ogg
@@ -223,5 +240,8 @@ rm -rf $RPM_BUILD_ROOT%{_datadir}/gnome/cursor-fonts
 %dir %{_datadir}/gnome-control-center/keybindings
 %dir %{_datadir}/gnome/wm-properties
 
+%files devel
+%{_datadir}/pkgconfig/gnome-keybindings.pc
+
 %changelog
 %autochangelog
