diff --git OLD/filesystem.spec NEW/filesystem.spec
index 015b29f..804f5bb 100644
--- OLD/filesystem.spec
+++ NEW/filesystem.spec
@@ -22,9 +22,6 @@ Requires(pre): setup
 Provides: basesystem = 12-%{release}
 Obsoletes: basesystem < 11-30
 
-Provides:   filesystem-afs = %{version}-%{release}
-Obsoletes:  filesystem-afs < 3.14-2
-
 %if %{with merged_sbin}
 # A virtual provides to indicate merged bin and sbin directories.
 # This is intended in particular for rpm-ostree, so it can conditionalize
@@ -54,6 +51,29 @@ This subpackage of filesystem package contains just the file with
 the directories owned by the filesystem package. This can be used
 during the build process instead of calling rpm -ql filesystem.
 
+%package games
+Summary: Games directories tree for the filesystem package
+Requires: filesystem = %{version}-%{release}
+
+%description games
+This subpackage of filesystem package contains the games directories
+
+%package debug
+Summary: Debugging directories tree for the filesystem package
+Requires: filesystem = %{version}-%{release}
+
+%description debug
+This subpackage of filesystem package contains the debugging directories
+tree.
+
+%package devel
+Summary: Development directories tree for the filesystem package
+Requires: filesystem = %{version}-%{release}
+
+%description devel
+This subpackage of filesystem package contains the development directories
+tree.
+
 %package srpm-macros
 Summary: Macros for the sbin-merge
 BuildArch: noarch
@@ -76,11 +96,11 @@ install -p -c -m755 %SOURCE3 %{buildroot}/iso_3166.sed
 cd %{buildroot}
 
 Paths=(
-        afs boot dev \
-        etc/{X11/{applnk,fontpath.d,xinit/{xinitrc,xinput}.d},xdg/autostart,opt,pm/{config.d,power.d,sleep.d},skel,sysconfig,keys/ima,pki,bash_completion.d,default,rwtab.d,statetab.d} \
-        home media mnt opt root run srv tmp \
-        usr/{bin,games,include,%{_lib}/{bpf,games,X11,pkgconfig,pm-utils/{module.d,power.d,sleep.d}},lib/{debug/{.dwz,usr},games,locale,modules,sysimage,systemd/{system,user},swidtag,sysusers.d,tmpfiles.d},libexec,local/{bin,etc,games,lib,%{_lib}/bpf,src,share/{applications,man/man{1,2,3,4,5,6,7,8,9,n,1x,2x,3x,4x,5x,6x,7x,8x,9x},info},libexec,include,},share/{aclocal,appdata,applications,augeas/lenses,backgrounds,bash-completion{,/completions,/helpers},desktop-directories,dict,doc,empty,fish/vendor_completions.d,games,gnome,help,icons,idl,info,licenses,man/man{1,2,3,4,5,6,7,8,9,n,1x,2x,3x,4x,5x,6x,7x,8x,9x,0p,1p,3p},metainfo,mime-info,misc,modulefiles,omf,pixmaps,pkgconfig,sounds,themes,xsessions,X11/fonts,wayland-sessions,zsh/site-functions},src,src/kernels,src/debug} \
-        var/{adm,empty,ftp,lib/{games,misc,rpm-state},local,log,nis,preserve,spool/{mail,lpd},tmp,db,cache/bpf,opt,games,yp}
+        boot dev \
+        etc/{X11,xdg/autostart,skel,sysconfig,keys/ima,pki,bash_completion.d,default,rwtab.d} \
+        home mnt root run tmp \
+        usr/{bin,games,include,%{_lib}/games,lib/{debug/{.dwz,usr},games,locale,modules,sysimage,systemd/{system,user},swidtag,sysusers.d,tmpfiles.d},libexec,local/{bin,etc,games,lib,%{_lib},src,share/{applications,man/man{1,2,3,4,5,6,7,8,9,n,1x,2x,3x,4x,5x,6x,7x,8x,9x},info},libexec,include},share/{appdata,applications,backgrounds,bash-completion/{completions,helpers},desktop-directories,dict,doc,empty,games,gnome,help,icons,ima,info,licenses,man/man{1,2,3,4,5,6,7,8,9,n,1x,2x,3x,4x,5x,6x,7x,8x,9x,0p,1p,3p},metainfo,misc,pixmaps,sounds,themes,X11,wayland-sessions},src,src/kernels,src/debug} \
+        var/{adm,empty,lib/{games,misc,rpm-state},local,log,preserve,spool/{mail,lpd},tmp,db,cache,games,opt}
 )
 for i in "${Paths[@]}"; do
     mkdir -p "$i"
@@ -102,6 +122,7 @@ ln -snf usr/lib usr/lib/debug/lib
 ln -snf usr/%{_lib} usr/lib/debug/%{_lib}
 ln -snf ../.dwz usr/lib/debug/usr/.dwz
 ln -snf usr/sbin usr/lib/debug/sbin
+ln -snf run/media media
 
 %if %{with merged_sbin}
 ln -snf bin usr/sbin
@@ -188,46 +209,31 @@ cp -p %{SOURCE7} %{buildroot}%{rpmmacrodir}/macros.filesystem
 posix.mkdir("/usr")
 posix.mkdir("/usr/bin")
 posix.mkdir("/usr/lib")
-posix.mkdir("/usr/lib/debug")
-posix.mkdir("/usr/lib/debug/usr/")
-posix.mkdir("/usr/lib/debug/usr/bin")
-posix.mkdir("/usr/lib/debug/usr/lib")
-posix.mkdir("/usr/lib/debug/usr/%{_lib}")
 posix.mkdir("/usr/%{_lib}")
 posix.mkdir("/usr/local")
 posix.mkdir("/usr/local/bin")
 posix.symlink("usr/bin", "/bin")
 posix.symlink("usr/sbin", "/sbin")
 posix.symlink("usr/lib", "/lib")
-posix.symlink("usr/bin", "/usr/lib/debug/bin")
-posix.symlink("usr/lib", "/usr/lib/debug/lib")
-posix.symlink("usr/%{_lib}", "/usr/lib/debug/%{_lib}")
-posix.symlink("../.dwz", "/usr/lib/debug/usr/.dwz")
-posix.symlink("usr/sbin", "/usr/lib/debug/sbin")
 posix.symlink("usr/%{_lib}", "/%{_lib}")
 if "%{_arch}" == "riscv64" then
   posix.symlink(".", "/usr/%{_lib}/lp64d")
 end
+posix.symlink("run/media", "/media")
 posix.mkdir("/run")
 posix.mkdir("/proc")
 posix.mkdir("/sys")
 posix.chmod("/proc", 0555)
 posix.chmod("/sys", 0555)
-st = posix.stat("/media")
-if st and st.type == "link" then
-  os.remove("/media")
-end
 posix.mkdir("/var")
 posix.symlink("../run", "/var/run")
 posix.symlink("../run/lock", "/var/lock")
 
 %if %{with merged_sbin}
 posix.symlink("bin", "/usr/sbin")
-posix.symlink("bin", "/usr/lib/debug/usr/sbin")
 posix.symlink("bin", "/usr/local/sbin")
 %else
 posix.mkdir("/usr/sbin")
-posix.mkdir("/usr/lib/debug/usr/sbin")
 %endif
 
 %if %{with merged_sbin}
@@ -240,6 +246,24 @@ end
 print('}')
 }
 
+%pretrans debug -p <lua>
+posix.mkdir("/usr/lib/debug")
+posix.mkdir("/usr/lib/debug/usr/")
+posix.mkdir("/usr/lib/debug/usr/bin")
+posix.mkdir("/usr/lib/debug/usr/lib")
+posix.mkdir("/usr/lib/debug/usr/%{_lib}")
+posix.symlink("usr/bin", "/usr/lib/debug/bin")
+posix.symlink("usr/lib", "/usr/lib/debug/lib")
+posix.symlink("usr/%{_lib}", "/usr/lib/debug/%{_lib}")
+posix.symlink("../.dwz", "/usr/lib/debug/usr/.dwz")
+posix.symlink("usr/sbin", "/usr/lib/debug/sbin")
+
+%if %{with merged_sbin}
+posix.symlink("bin", "/usr/lib/debug/usr/sbin")
+%else
+posix.mkdir("/usr/lib/debug/usr/sbin")
+%endif
+
 %filetriggerin -p <lua> -- /usr/bin
 --# This implements filesystem(unmerged-sbin-symlinks) feature.
 st = posix.stat("/usr/sbin")
@@ -399,9 +423,13 @@ end
 %posttrans -p <lua>
 --# we need to restorecon on some dirs and symlinks created in %pretrans or by other packages
 if posix.access ("/usr/sbin/restorecon", "x") then
-  rpm.execute("/usr/sbin/restorecon", "/var", "/var/run", "/var/lock", "/sys", "/boot", "/dev",
-              "/media", "/afs", "/usr/sbin", "/usr/local/sbin")
-  rpm.execute("/usr/sbin/restorecon", "-r", "/usr/lib/debug")
+  rpm.execute("/usr/sbin/restorecon", "/var", "/var/run", "/var/lock", "/sys", "/dev", "/usr/sbin", "/usr/local/sbin")
+  if posix.access ("/boot", "x") then
+    rpm.execute("/usr/sbin/restorecon", "/boot")
+  end
+  if posix.access ("/media", "x") then
+    rpm.execute("/usr/sbin/restorecon", "/media")
+  end
 end
 
 %if %{with merged_sbin}
@@ -411,10 +439,46 @@ end
 %merge_scriptlet
 %endif
 
+%posttrans debug -p <lua>
+if posix.access ("/usr/sbin/restorecon", "x") then
+  rpm.execute("/usr/sbin/restorecon", "-r", "/usr/lib/debug")
+end
+
 %files content
 %dir %{_datadir}/filesystem
 %{_datadir}/filesystem/paths
 
+%files games
+/usr/games
+%attr(555,root,root) /usr/lib/games
+%ifarch x86_64 ppc64 sparc64 s390x aarch64 ppc64le mips64 mips64el riscv64
+%attr(555,root,root) /usr/%{_lib}/games
+%endif
+/usr/local/games
+/usr/share/games
+/var/games
+/var/lib/games
+
+%files debug
+%dir /usr/lib/debug
+%dir /usr/lib/debug/.dwz
+%ghost /usr/lib/debug/bin
+%ghost /usr/lib/debug/lib
+%ghost /usr/lib/debug/%{_lib}
+%ghost %dir /usr/lib/debug/usr
+%ghost /usr/lib/debug/usr/bin
+%ghost /usr/lib/debug/usr/sbin
+%ghost /usr/lib/debug/usr/lib
+%ghost /usr/lib/debug/usr/%{_lib}
+%ghost /usr/lib/debug/usr/.dwz
+%ghost /usr/lib/debug/sbin
+
+%files devel
+/usr/include
+/usr/local/include
+/usr/local/src
+/usr/src
+
 %files srpm-macros
 %{_datadir}/filesystem/sbin-filenames
 %{_fileattrsdir}/filesystem.attr
@@ -426,14 +490,11 @@ end
 %dir %attr(555,root,root) /
 /bin
 /sbin
-%attr(555,root,root) /boot
-%attr(555,root,root) /afs
+%attr(555,root,root) %ghost /boot
 /dev
 %dir /etc
-/etc/X11
+%ghost /etc/X11
 /etc/xdg
-/etc/opt
-/etc/pm
 /etc/skel
 /etc/sysconfig
 /etc/keys
@@ -441,19 +502,16 @@ end
 /etc/bash_completion.d/
 %dir /etc/default
 %dir /etc/rwtab.d
-%dir /etc/statetab.d
 /home
 /lib
 %ifarch x86_64 ppc64 sparc64 s390x aarch64 ppc64le mips64 mips64el riscv64
 /%{_lib}
 %endif
-/media
+%ghost /media
 %dir /mnt
-%dir /opt
 %ghost %attr(555,root,root) /proc
 %attr(550,root,root) /root
 /run
-/srv
 %ghost %attr(555,root,root) /sys
 %attr(1777,root,root) /tmp
 %dir /usr
@@ -463,8 +521,6 @@ end
 %else
 %attr(555,root,root) /usr/sbin
 %endif
-/usr/games
-/usr/include
 %dir %attr(555,root,root) /usr/lib
 %dir /usr/lib/swidtag
 %dir /usr/lib/sysimage
@@ -475,30 +531,12 @@ end
 %dir /usr/lib/tmpfiles.d
 %dir /usr/lib/locale
 %dir /usr/lib/modules
-%dir /usr/lib/debug
-%dir /usr/lib/debug/.dwz
-%ghost /usr/lib/debug/bin
-%ghost /usr/lib/debug/lib
-%ghost /usr/lib/debug/%{_lib}
-%ghost %dir /usr/lib/debug/usr
-%ghost /usr/lib/debug/usr/bin
-%ghost /usr/lib/debug/usr/sbin
-%ghost /usr/lib/debug/usr/lib
-%ghost /usr/lib/debug/usr/%{_lib}
-%ghost /usr/lib/debug/usr/.dwz
-%ghost /usr/lib/debug/sbin
-%attr(555,root,root) /usr/lib/games
 %ifarch x86_64 ppc64 sparc64 s390x aarch64 ppc64le mips64 mips64el riscv64
 %dir %attr(555,root,root) /usr/%{_lib}
-%attr(555,root,root) /usr/%{_lib}/games
 %endif
-%attr(555,root,root) /usr/%{_lib}/bpf
-%attr(555,root,root) /usr/%{_lib}/X11
-%attr(555,root,root) /usr/%{_lib}/pm-utils
 %ifarch riscv64
 /usr/%{_lib}/lp64d
 %endif
-/usr/%{_lib}/pkgconfig
 /usr/libexec
 %dir /usr/local
 %if %{with merged_sbin}
@@ -508,16 +546,11 @@ end
 %endif
 /usr/local/etc
 /usr/local/bin
-/usr/local/games
-/usr/local/include
 /usr/local/lib*
 /usr/local/share
-/usr/local/src
 %dir /usr/share
-/usr/share/aclocal
 /usr/share/appdata
 /usr/share/applications
-/usr/share/augeas
 /usr/share/backgrounds
 %dir /usr/share/bash-completion
 /usr/share/bash-completion/completions
@@ -526,54 +559,41 @@ end
 /usr/share/dict
 /usr/share/doc
 %attr(555,root,root) %dir /usr/share/empty
-/usr/share/fish
-/usr/share/games
-/usr/share/gnome
+%ghost /usr/share/gnome
 /usr/share/help
 /usr/share/icons
-/usr/share/idl
+/usr/share/ima
 /usr/share/info
 %dir /usr/share/licenses
 %dir /usr/share/locale
 %dir /usr/share/man
 /usr/share/metainfo
-/usr/share/mime-info
 /usr/share/misc
-%dir /usr/share/modulefiles
-/usr/share/omf
 /usr/share/pixmaps
-/usr/share/pkgconfig
 /usr/share/sounds
 /usr/share/themes
-/usr/share/xsessions
 %dir /usr/share/X11
-/usr/share/X11/fonts
 /usr/share/wayland-sessions
-/usr/share/zsh
-/usr/src
-/usr/tmp
+%ghost /usr/tmp
 %dir /var
 /var/adm
 %dir /var/cache
-/var/cache/bpf
-/var/db
+%ghost /var/db
 /var/empty
-/var/ftp
-/var/games
-/var/lib
+%dir /var/lib
+/var/lib/misc
+/var/lib/rpm-state
 /var/local
 %ghost /var/lock
 /var/log
 /var/mail
-/var/nis
-/var/opt
+%dir /var/opt
 /var/preserve
 %ghost /var/run
 %dir /var/spool
 %attr(755,root,root) /var/spool/lpd
 %attr(775,root,mail) /var/spool/mail
 %attr(1777,root,root) /var/tmp
-/var/yp
 
 %changelog
 %autochangelog
