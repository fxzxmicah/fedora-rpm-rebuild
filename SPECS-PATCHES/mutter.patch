diff --git OLD/mutter.spec NEW/mutter.spec
index 302d160..945fe84 100644
--- OLD/mutter.spec
+++ NEW/mutter.spec
@@ -17,6 +17,9 @@
 %global major_version %%(echo %{version} | cut -d '.' -f1 | cut -d '~' -f 1)
 %global tarball_version %%(echo %{version} | tr '~' '.')
 
+# Disable libwacom for all builds
+%bcond libwacom 0
+
 Name:          mutter
 Version:       50.4
 Release:       %autorelease
@@ -27,6 +30,7 @@ License:       GPL-2.0-or-later
 URL:           http://www.gnome.org
 Source0:       http://download.gnome.org/sources/%{name}/%{major_version}/%{name}-%{tarball_version}.tar.xz
 
+Patch:         mutter-writable-system-config-native-gdctl.patch
 
 BuildRequires: cvt
 BuildRequires: desktop-file-utils
@@ -42,7 +46,10 @@ BuildRequires: pkgconfig(gobject-introspection-1.0) >= %{gobject_introspection_v
 BuildRequires: pkgconfig(sm)
 BuildRequires: pkgconfig(lcms2) >= %{lcms2_version}
 BuildRequires: pkgconfig(libadwaita-1)
+BuildRequires: pkgconfig(libevdev)
+%if %{with libwacom}
 BuildRequires: pkgconfig(libwacom)
+%endif
 BuildRequires: pkgconfig(xkbcommon)
 BuildRequires: pkgconfig(glesv2)
 BuildRequires: pkgconfig(graphene-gobject-1.0)
@@ -51,7 +58,6 @@ BuildRequires: pkgconfig(libpipewire-0.3) >= %{pipewire_version}
 BuildRequires: pkgconfig(sysprof-capture-4)
 BuildRequires: pkgconfig(libsystemd)
 BuildRequires: pkgconfig(umockdev-1.0)
-BuildRequires: python3-argcomplete
 BuildRequires: python3-docutils
 # Bootstrap requirements
 BuildRequires: gettext-devel git-core
@@ -88,7 +94,7 @@ Requires: libinput%{?_isa} >= %{libinput_version}
 Requires: pipewire%{_isa} >= %{pipewire_version}
 Requires: startup-notification
 Requires: dbus
-Requires: python3-argcomplete
+Requires: xorg-x11-server-Xwayland%{?_isa} >= 1.20.99.1
 
 # Need common
 Requires: %{name}-common = %{version}-%{release}
@@ -161,7 +167,14 @@ Viewer for nested mutter instances.
 %autosetup -S git -n %{name}-%{tarball_version}
 
 %build
-%meson -Degl_device=true
+%meson \
+    -Degl_device=true \
+%if %{with libwacom}
+    -Dlibwacom=true \
+%else
+    -Dlibwacom=false \
+%endif
+    %{nil}
 %meson_build
 
 %install
@@ -173,7 +186,6 @@ Viewer for nested mutter instances.
 %license COPYING
 %doc NEWS
 %{_bindir}/gdctl
-%{_bindir}/gnome-service-client
 %{_bindir}/mutter
 %{_datadir}/polkit-1/actions/org.gnome.mutter.*.policy
 %{_libdir}/lib*.so.*
@@ -183,7 +195,6 @@ Viewer for nested mutter instances.
 %{_libexecdir}/mutter-x11-frames
 %{_mandir}/man1/mutter.1*
 %{_mandir}/man1/gdctl.1*
-%{_mandir}/man1/gnome-service-client.1*
 %{bash_completions_dir}/gdctl
 
 %files common
@@ -205,10 +216,12 @@ Viewer for nested mutter instances.
 %{_libexecdir}/installed-tests/mutter-%{mutter_api_version}
 
 %files devkit
+%{_bindir}/gnome-service-client
 %{_datadir}/applications/org.gnome.Mutter.Mdk.desktop
 %{_datadir}/glib-2.0/schemas/org.gnome.mutter.devkit.gschema.xml
 %{_datadir}/icons/hicolor/*/apps/org.gnome.Mutter.Mdk*
 %{_libexecdir}/mutter-devkit
+%{_mandir}/man1/gnome-service-client.1*
 
 %changelog
 %autochangelog
