diff --git OLD/policycoreutils.spec NEW/policycoreutils.spec
index 2005ac8..df7ef6d 100644
--- OLD/policycoreutils.spec
+++ NEW/policycoreutils.spec
@@ -46,6 +46,7 @@ Patch0003: 0003-sandbox-Use-matchbox-window-manager-instead-of-openb.patch
 Patch0004: 0004-Use-SHA-2-instead-of-SHA-1.patch
 Patch0005: 0005-python-sepolicy-Fix-spec-file-dependencies.patch
 Patch0006: 0006-sepolicy-Fix-detection-of-writeable-locations.patch
+Patch9001: https://gist.github.com/fxzxmic/33ac12a37234298dbd06c983edb49abc/raw/4b11865fcc49d1a3265c4f7208d2643cc68f6c19/selinux-policycoreutils-fixfiles.patch
 # Patch list end
 
 # gen_changelog
@@ -77,8 +78,8 @@ BuildRequires: (python3-wheel if python3-setuptools < 71)
 BuildRequires: systemd
 BuildRequires: git-core
 BuildRequires: gnupg2
-Requires: util-linux grep gawk diffutils sed
-Requires: libsepol >= %{libsepolver} coreutils libselinux-utils >=  %{libselinuxver}
+# Requires: util-linux grep gawk diffutils sed coreutils
+Requires: libsepol >= %{libsepolver} libselinux-utils >=  %{libselinuxver}
 Recommends: rpm
 
 %description
@@ -276,6 +277,7 @@ by python 3 in an SELinux environment.
 Summary: SELinux policy core policy devel utilities
 Requires: policycoreutils-python-utils = %{version}-%{release}
 Requires: /usr/bin/make python3-dnf
+Requires: policycoreutils-semodule-utils = %{version}-%{release}
 Requires: (selinux-policy-devel if selinux-policy)
 
 %description devel
@@ -300,6 +302,25 @@ The policycoreutils-devel package contains the management tools use to develop p
 %{_usr}/share/bash-completion/completions/sepolicy
 
 
+%package semodule-utils
+Summary: SELinux semodule utilities
+Requires: policycoreutils = %{version}-%{release}
+
+%description semodule-utils
+The policycoreutils-semoudle-utils package contains the semodule utilities
+to manage SELinux policy modules.
+
+%files semodule-utils
+%{_bindir}/semodule_expand
+%{_bindir}/semodule_link
+%{_bindir}/semodule_package
+%{_bindir}/semodule_unpackage
+%{_mandir}/man8/semodule_expand.8*
+%{_mandir}/man8/semodule_link.8*
+%{_mandir}/man8/semodule_unpackage.8*
+%{_mandir}/man8/semodule_package.8*
+
+
 %package sandbox
 Summary: SELinux sandbox utilities
 Requires: python3-policycoreutils = %{version}-%{release}
@@ -388,10 +409,6 @@ system-config-selinux is a utility for managing the SELinux environment
 %{_sbindir}/sestatus
 %endif
 %{_bindir}/secon
-%{_bindir}/semodule_expand
-%{_bindir}/semodule_link
-%{_bindir}/semodule_package
-%{_bindir}/semodule_unpackage
 %{_bindir}/sestatus
 %{_libexecdir}/selinux/hll
 %{_libexecdir}/selinux/selinux-autorelabel
@@ -413,10 +430,6 @@ system-config-selinux is a utility for managing the SELinux environment
 %{_mandir}/man1/secon.1*
 %{_mandir}/man1/unsetfiles.1*
 %{_mandir}/man8/genhomedircon.8*
-%{_mandir}/man8/semodule_expand.8*
-%{_mandir}/man8/semodule_link.8*
-%{_mandir}/man8/semodule_unpackage.8*
-%{_mandir}/man8/semodule_package.8*
 %dir %{_datadir}/bash-completion
 %{_datadir}/bash-completion/completions/setsebool
 %{!?_licensedir:%global license %%doc}
